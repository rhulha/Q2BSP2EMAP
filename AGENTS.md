This projects goal is to convert Quake 2 single player maps (`.bsp`) into Prodeus `.emap` files, including material setup from neural-upscaled textures.

The docs folder contains what I learned about Prodeus maps so far.

